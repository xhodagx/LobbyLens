<UserControl x:Class="LobbyLens.LobbyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="GlyphBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF9DA2A6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="18"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bg" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#22FFFFFF"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="RootGrid" HorizontalAlignment="Left" VerticalAlignment="Top">
        <Grid.RenderTransform>
            <ScaleTransform x:Name="RootScale" ScaleX="1" ScaleY="1"/>
        </Grid.RenderTransform>
        <Border x:Name="PanelBorder"
                Background="#F014161A"
                BorderBrush="#3DFFFFFF"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10,6,10,8"
                MinWidth="190"
                MouseLeftButtonDown="PanelBorder_MouseLeftButtonDown"
                MouseLeftButtonUp="PanelBorder_MouseLeftButtonUp"
                MouseLeave="PanelBorder_MouseLeave"
                MouseMove="PanelBorder_MouseMove"
                MouseWheel="PanelBorder_MouseWheel">
            <Border.Effect>
                <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.55"/>
            </Border.Effect>
            <StackPanel>
                <Grid Margin="0,0,0,4">
                    <TextBlock Text="OPPONENT MMR" Foreground="#FFD4B16A" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" Margin="0,0,48,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="GearButton" Content="⚙" Click="GearButton_Click" Style="{StaticResource GlyphBtn}" ToolTip="LobbyLens settings"/>
                        <Button x:Name="MinimizeButton" Content="–" Click="MinimizeButton_Click" Style="{StaticResource GlyphBtn}" Margin="4,0,0,0"/>
                        <Button x:Name="CloseButton" Content="✕" Click="CloseButton_Click" Style="{StaticResource GlyphBtn}" Margin="4,0,0,0"/>
                    </StackPanel>
                </Grid>
                <StackPanel x:Name="RowsHost"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
